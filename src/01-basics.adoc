include::header.inc.adoc[]


== Teil 1 - Grundlagen

Im ersten Teil des Praktikums geht es um die Einrichtung und das Kennenlernen
der Werkzeuge *GitHub*, *Git*, *Visual Studio Code* und *AsciiDoc*.

:imageTitle: Meanwhile, in the Classroom...
:imageFile: comic_basics.jpg
.{imageTitle}
image::{imageFile}["{imageTitle}", width=700, title="{imageTitle}"]

include::learning-goals.inc.adoc[tag=01-basics]

{section-separator}

=== 1. GitHub

==== Aufgabe 1.1 - Login

. Loggen Sie sich auf der Webseite https://github.com/[github.com] ein.
   Sollten Sie noch keinen Account haben, kann er über
   https://github.com/join[github.com/join] erstellt werden.
+
Sie können entweder einen neuen Account für Ihre HTWD-E-Mail-Adresse erstellen
oder die HTWD-E-Mail-Adresse zu Ihrem bestehenden Account unter „Settings
> Emails“ hinzufügen.

TIP: Bei Verwendung einer HTWD-E-Mail-Adresse erhalten Sie über das
https://github.com/education/students[GitHub Education]-Programm Zugriff auf
weitere GitHub-Features und können an zusätzlichen Bonusprogrammen teilnehmen.

==== Aufgabe 1.2 - Repository anlegen

. Erstellen Sie ein neues Repository mit folgenden Einstellungen:

** Repository name: `htwd-se-example-project`
** Description: `Beispielprojekt für die Praktika in Software Engineering`
** Choose visibility: `Private` (oder `Public`)
** Add README: `On` (erster Inhalt im Repository)

NOTE: Die _README.md_ enthält eine kurze Beschreibung des Projekts sowie wichtige
Informationen zur Nutzung und Bearbeitung des Repositories. Sie ist im
https://markdown.de/[Markdown]-Format geschrieben und wird auf GitHub
automatisch als Projektbeschreibung angezeigt.

==== Aufgabe 1.3 - Repository ändern

. Bearbeiten Sie die Datei *_README.md_* direkt über die GitHub-Webseite und
  ersetzen Sie den Inhalt durch Folgendes:
+
--
.README.md
[source,markdown]
----
# Beispielprojekt

Ein erstes Projekt für die Praktika im Fach Software Engineering.

## Inhalt

Dieses Projekt dient als Übungsumgebung für die Praktikumsaufgaben.

- Versionsverwaltung mit **Git**
- Dokumentation mit **AsciiDoc**
- Verwendung von **GitHub**
----

* Hinweise zum Markdown-Support von GitHub: https://docs.github.com/en/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax[Basic writing and formatting syntax]
--

. Nehmen Sie die Änderung mit kbd:[Commit changes...] als neue Version in Ihr
  Repository auf.


{section-separator}

=== 2. Git

[#section_git_config]
==== Aufgabe 2.1 - Konfiguration

Für die Verwendung von Git auf dem lokalen Rechner wird eine Konfiguration
benötigt, damit Git weiß, wer die Änderungen am Repository vornimmt. Hierfür
müssen ein Name, eine E-Mail und eventuell weitere Einstellungen angegeben
werden.

Die angegebene E-Mail dient zur Identifizierung Ihrer Änderungen am Repository
und zur Zuordnung Ihres GitHub-Accounts.
// Der Proxy ist notwendig, damit Sie aus dem geschützten HTWD-Netz (Labore, VPN,
// WLAN) sich mit dem Internet verbinden können.

Die globale Konfiguration von Git wird auf Ihrem Rechner im
Benutzerverzeichnis, siehe <<table_git-config_speicherort>>, in der Datei
`.gitconfig` abgelegt. Sie gilt global für alle Ihre Git-Repositories.

[#table_git-config_speicherort]
.Speicherort der globalen Git-Konfiguration
[cols="1,3", width="60%"]
|===
| Linux   | `~/.gitconfig` (`~/.config/git/config`)
| macOS   | `~/.gitconfig` (`~/.config/git/config`)
| Windows | `C:\Users\<username>\.gitconfig`
|===

[TIP]
====
Arbeiten Sie auf Ihrem (privaten) Rechner mit verschiedenen Git-Identitäten
(andere E-Mail für Privat, Studium, Arbeit, ...), können Sie auch spezifische
Konfigurationen je Repository verwenden.

- Siehe: https://www.informatik.htw-dresden.de/~zirkelba/praktika/se/git-docs-collab/faq/index.html#_was_ist_bei_mehreren_git_identit%C3%A4ten_zu_beachten[FAQ: Was ist bei mehreren Git-Identitäten zu beachten?]
====

. _Nur für den privaten Rechner notwendig_: Starten Sie Visual Studio Code und
  öffnen Sie über kbd:[Ctrl+Shift+G] ("View > SCM") den Bereich des Source
  Control Managements, welcher für die Versionsverwaltung zuständig ist. Taucht
  hier eine Meldung ähnlich wie: "_A valid git installation was not detected
  ... Please install git ..._" auf, schauen Sie bitte unter <<Vorbereitung
  privater Rechner>> nach, ob Sie Git installiert haben.

. Öffnen Sie Ihren Dateimanager und schauen Sie nach, ob es schon eine
  _.gitconfig_ Datei gibt (Siehe <<table_git-config_speicherort>>).

. Öffnen Sie in Visual Studio Code über "File > Open..." die schon vorhandene
  Datei _.gitconfig_ oder legen Sie eine neue Datei über "File > New File" an.
  Passen Sie den Inhalt wie folgt an:
+
--
.Inhalt der .gitconfig
[source,ini]
----
[user]
  name = Vorname Nachname
  email = vorname.nachname@stud.htw-dresden.de <1>

# Nur für Netzlaufwerke der HTWD Laborrechner notwendig,
# kann auf privaten Rechnern weggelassen werden.
[safe]
  directory = * <2>
----
<1> Für die E-Mail verwenden Sie bitte eine Ihrer HTWD Adressen (bspw.
    `s00000@htw-dresden.de` oder `vorname.nachname@stud.htw-dresden.de`), welche in
    Ihrem GitHub-Account verwendet wird.
<2> Erlaubt es Git, Repositories auf (Netz-)Laufwerken mit unterschiedlichen
    Eigentümer-Einstellungen trotz `fatal: unsafe repository`-Sicherheitsmeldung zu
    nutzen. +
    Dies in den Windows-Laboren der HTWD der Fall, wenn Sie auf Ihrem
    Studentenlaufwerk (`U:`) arbeiten.

.Hinweise: Git-Meldung: `fatal: unsafe repository`
[%collapsible]
====
=====
Git verweigert, seit Version 2.35.2, aus Sicherheitsgründen (Security Fix
https://github.blog/2022-04-12-git-security-vulnerability-announced/#cve-2022-24765[CVE-2022-24765])
die Nutzung von Repositories auf (Netz-)Laufwerken mit unterschiedlichen
Eigentümer-Einstellungen. Möchte man diese trotzdem nutzen, muss dies über die
Konfiguration (Einstellung:
https://git-scm.com/docs/git-config/2.35.2#Documentation/git-config.txt-safedirectory[safe.directory])
von Git explizit erlaubt werden.

.Für alle betreffenden Repositories erlauben:
Mit `git config --global --add safe.directory *` oder direkt über die
_.gitconfig_ muss folgendes hinzugefügt werden:
[source,ini]
----
[safe]
  directory = * <1>
----
<1> Der `*`-Stern dient als Platzhalter für alle betreffenden Repositories,
    egal in welchem Verzeichnispfad sie sich befinden.

.Nur ein bestimmtes Repository erlauben:
. Terminal öffnen, bspw. in VS Code: "View > Terminal"
. Zum passenden Repository-Verzeichnis navigieren `cd path/to/repo`
. Im Terminal `git status` ausführen
+
[source,console]
----
fatal: unsafe repository ('//141.56...)
To add an exception for this directory, call:

        git config --global --add safe.directory '%(prefix)///141.56...
----
. Befehl `git config --global --add safe.directory...` aus obiger Meldung ausführen
. Im Terminal `git status` noch einmal ausführen
=====
====
--

. Speichern Sie die gemachten Änderungen über "File > Save" bzw. kbd:[strg+s]
  oder kbd:[cmd+s].
+
_Ist die Datei neu_: Speichern Sie die *neue Datei* über "File > Save As..."
und wählen Sie als Datei-Typ/Format: *All files* aus. Geben Sie jetzt den
Dateinamen `.gitconfig` an und speichern Sie in Ihrem Benutzerverzeichnis
(Siehe <<table_git-config_speicherort>>).

. Zum Testen der Konfiguration öffnen Sie in Visual Studio Code über "View
  > Terminal"  bzw. kbd:[Ctrl+ö] oder kbd:[Ctrl+Shift+´] das integrierte
  Terminal oder ein beliebiges anderes Terminal (Windows: PowerShell) und geben
  folgende Git-Kommandos ein:
+
.Anzeige des festgelegten Namens:
[source, console]
----
$ git config --global user.name
Vorname Nachname
----
+
.Anzeige der festgelegten E-Mail:
[source, console]
----
$ git config --global user.email
vorname.nachname@stud.htw-dresden.de
----

. Sichern Sie die funktionierende Git-Konfiguration in Ihr Studentenlaufwerk,
  bspw. unter `U:\praktika\se\.gitconfig`, damit diese bei einem neuen Anmelden
  in den Windows-Laboren schnell wiederhergestellt werden kann.

.Hinweise: Alternative Git-Konfiguration per Git-Kommando
[%collapsible]
====
=====
Die Git-Konfiguration kann ebenfalls direkt mit den Git-Kommandos `git config
[--global] ...` angelegt/bearbeitet, mit `git config --list` angezeigt und
Einstellungen mit `git config [--global] --unset ...` entfernt werden.

[source, console]
----
$ git config --global user.name "Vorname Nachname"
$ git config --global user.email vorname.nachname@stud.htw-dresden.de
----

Für die Labore der HTWD zusätzlich noch ein:

[source, console]
----
$ git config --global --add safe.directory *
----
=====
====

.Hinweise: Einbindung des Rechenzentrum-Netzlaufwerkes Windows
[%collapsible]
====
=====
.icon:windows[] Windows
. *Windows Explorer* (kbd:[icon:windows[\]-Taste + e]) öffnen
. Seitenleiste: *Netzwerk* -> Rechtsklick: *Netzlaufwerk verbinden...*
. Netzwerkordner angeben:
** Ordner: `\\samba.htw-dresden.de\sXXXXX` -> kbd:[Fertig stellen]
. Netzwerkanmeldeinformationen eingeben:
** Benutzername: `dom\sXXXXX`, Kennwort: `...` -> kbd:[OK]

.icon:apple[] macOS
. *Finder* öffnen
. "Menü > Gehe zu > Mit Server verbinden ..."
. Daten eingeben:
** Ordner: `smb://samba.htw-dresden.de/sXXXXX`
** Name: `dom\sXXXXX`
** Passwort: `...`

.icon:linux[] Linux #TODO#
...
=====
====

==== Aufgabe 2.2 - Repository Klonen

https://docs.github.com/en/repositories/creating-and-managing-repositories/cloning-a-repository[Clonen]
Sie Ihr Repository *htwd-se-example-project* in Ihr Studentenlaufwerk, bspw.
unter `U:\praktika\se\`, oder an geeigneter Stelle lokal auf Ihren Rechner.

.Repository-URL:
----
https://github.com/<account>/htwd-se-example-project.git
----

Zu finden auf GitHub in Ihrem Repository beim Klick auf den grünen kbd:[Code]
Button im Bereich HTTPS.

.Variante - Visual Studio Code (HTTPS)
[%collapsible]
====
=====
. In Visual Studio Code kann über folgende Varianten ein Git-Repository geklont
  werden:
** "View > Source Control" -> Button kbd:[Clone Repository]
** "View > Command Palette..." -> `Git: Clone`
** "Get Started"-Seite -> Start: _Clone Git Repository..._
** "View > Terminal" -> `cd path/to/folder`  -> `git clone ...`
+
Falls in VS Code schon ein Repository geöffnet ist, kann dies auch in "View
> SCM" über die drei Punkte "*...*" im oberen Bereich mit dem Menüeintrag
*Clone* erfolgen.
. Im oberen mittleren Bereich geht jetzt ein Eingabefenster auf, in welches Sie
  Ihre Repository-URL kopieren und Enter drücken.
. Wählen Sie noch einen Zielordner, in welchem das Repository als Verzeichnis
  gespeichert werden soll.
. Es erfolgt die Abfrage der Authentifizierung mit GitHub. Hier gibt es jetzt
  je nach Installation verschiedene Varianten. Entweder wird der Login per
  *GitHub OAuth* und der *Access Token* abgefragt oder Visual Studio Code nutzt
  seine *Access to GitHub* Erweiterung.
+
Verwenden Sie: *GitHub OAuth*, welches die Authentifizierung über den Webbrowser ermöglicht.

[NOTE]
======
Die Kommunikation Ihres Repositories per *_https_* mit GitHub verlangt jedes
Mal erneut den Login. Deswegen ist in den Windows-Laboren die Option mit `git
config --global credential.helper=manager` schon in der Git-Konfiguration
gesetzt. Damit bleibt Ihr Login für die aktuelle Sitzung gespeichert.
Alternativ können Sie auch einen Timeout mit `git config --global
credential.helper 'cache --timeout=5400'` einstellen, so würde der Login 60
Minuten gespeichert bleiben.
======
=====
====

.Variante - Terminal (HTTPS)
[%collapsible]
====
=====
. Öffnen Sie ein Terminal (Windows: PowerShell)
. Führen Sie darin folgendes Kommando aus und verwenden Sie, bei der Nachfrage
  zur Authentication-Methode Nummer *2* für den Personal access token von
  GitHub:
+
--
.Clone-Befehl:
[source,sh]
----
git clone https://github.com/<account>/htwd-se-example-project.git
----

.Ausgabe icon:windows[] Windows - PowerShell:
[source,console]
----
> git clone https://github.com/<account>/htwd-se-example-project.git
Cloning into 'htwd-se-example-project'...
Select an authentication method for 'https://github.com/':
  1. Web browser (default)
  2. Personal access token
option (enter for default): 2
Enter GitHub personal access token for 'https://github.com/'...
Token:
remote: Enumerating objects: 10, done.
remote: Counting objects: 100% (10/10), done.
remote: Compressing objects: 100% (8/8), done.
remote: Total 10 (delta 0), reused 4 (delta 0), pack-reused 0
Receiving objects: 100% (10/10), done.
----

.Ausgabe icon:linux[] Linux und icon:apple[] macOS - Terminal:
[source,console]
----
$ git clone https://github.com/<account>/htwd-se-example-project.git
Klone nach 'htwd-se-example-project' ...
Username for 'https://github.com': <account>
Password for 'https://<account>@github.com':
remote: Enumerating objects: 10, done.
remote: Counting objects: 100% (10/10), done.
remote: Compressing objects: 100% (8/8), done.
remote: Total 10 (delta 0), reused 4 (delta 0), pack-reused 0
Receiving objects: 100% (10/10), done.
----
--
=====
====

.Hinweise: Anlegen eines Access Token (Classic)
[%collapsible]
====
=====
Als Alternative zum Login mit Passwort des GitHub-Accounts, kann auch ein *Personal
Access Token* verwendet werden. Mit den *Tokens* (Personal access tokens)
können, ähnlich wie bei  *GitHub OAuth*, Anwendungen gezielt eingeschränkte
Zugriffsmöglichkeiten gegeben oder entzogen werden, ohne das Accountpasswort
preiszugeben.

Anleitung für das Anlegen von Access Token:
https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token[Creating a personal access token]

. Legen Sie auf GitHub über "Settings > Developer settings > Personal access
  tokens" einen neuen Token bspw. `Praktikum SE I` mit einer Gültigkeit für
  dieses Semester und nur dem Scope *repo* und *workflow* an.
+
--
WARNING: Der Personal access token ist nur nach dem Erstellen zu sehen und kann
danach nicht wieder angezeigt werden!

In den Laboren (Windows) werden Ihre Profile nicht gespeichert, was zum Verlust
des verwendeten Passwortes/Tokens nach dem Abmelden führt, weswegen wir zu
jedem Praktikum wiederholt diesen Token für die (erste) Kommunikation mit Git
zu GitHub benötigen.

* Möglichkeit 1: Sie speichern den generierten Token an geeigneter Stelle ab,
  um diesen in jedem Praktikum wiederholt verwenden zu können.
* Möglichkeit 2: Sie lassen sich zu jedem Praktikumsbeginn den vorhandenen
  Token über kbd:[Regenerate token] neu generieren.
--

[NOTE]
======
Seit dem 13. August 2021 akzeptiert GitHub für die Authentifizierung von
Git-Operationen über HTTPS nur noch *GitHub OAuth* oder *Personal access
tokens* statt des Account Passwortes.
(https://github.blog/2020-12-15-token-authentication-requirements-for-git-operations/[Quelle])

.Hinweise: Fehlermeldung
[%collapsible]
=======
// https://stackoverflow.com/questions/68775869/support-for-password-authentication-was-removed-please-use-a-personal-access-to

.Eine Verwendung des Passwortes über HTTPS führt zu folgendem Fehler:
----
remote: Support for password authentication was removed on August 13, 2021. Please use a personal access token instead.
remote: Please see https://github.blog/2020-12-15-token-authentication-requirements-for-git-operations/ for more information.
----
=======
======
=====
====


.Hinweise: Git-Kommunikation per SSH und SSH-Key
[%collapsible]
====
=====
Eine weitere Variante wäre die Kommunikation mit GitHub per *_ssh_* statt
_https_ unter Nutzung eines SSH-Keys. Informationen zur Verwendung findet man
unter:
https://help.github.com/en/articles/connecting-to-github-with-ssh[Connecting to
GitHub with SSH].

.Variante - Terminal per SSH (SSH-Key bei GitHub hinterlegt):
[source,sh]
----
git clone git@github.com:<account>/htwd-se-example-project.git
----

Die Verwendung von ssh ist nur in den Linux-Laboren empfohlen, da unter Windows
die Einstellungen in den Nutzerprofilen nicht gespeichert werden.
=====
====


{section-separator}

=== 3. VS Code, AsciiDoc

==== Aufgabe 3.1 - Anpassung von VS Code

. Öffnen Sie Visual Studio Code.

. Gehen Sie in Visual Studio Code in die *Extensions* Ansicht und suchen Sie
  nach `AsciiDoc`. Installieren Sie, falls noch nicht vorhanden, die
  Erweiterung
  https://marketplace.visualstudio.com/items?itemName=asciidoctor.asciidoctor-vscode[AsciiDoc]
  von _asciidoctor_.

. Passen Sie in Visual Studio Code über "Preferences (oder Zahnradsymbol)>
  Settings > Extensions > asciidoc" folgende Einstellungen an:
** Asciidoc > Preview: *Use Editor Style*: icon:square-o[] (deaktiviert), für
   eine neutrale Vorschau


==== Aufgabe 3.2 - AsciiDoc Grundsyntax

. Öffnen Sie das eben geklonte Projekt (Repository), als Ordner in Visual
  Studio Code über "File > Open Folder..."

. Erstellen Sie in Ihrem lokalen Repository ein Verzeichnis *docs* und legen
  Sie in diesem eine neue leere Datei *_Beispieldokumentation.adoc_* an. Dies
  können Sie direkt in Visual Studio Code in der Explorer Ansicht über ein
  Rechtsklick (Kontextmenü: _New File_, _New Folder_) tun.
// +
// NOTE: Unter *Windows* muss in Visual Studio Code beim Speichern der Dateityp:
// `+All files *.*+` ausgewählt sein, damit die Datei die Endung _.adoc_ und nicht
// _.adoc.txt_ bekommt.

. Übernehmen Sie in die Datei _Beispieldokumentation.adoc_ das folgende
  AsciiDoc-Beispiel und passen Sie den Autor an Ihren Namen und das Datum an
  das heutige an.
+
--
Infos zur Syntax:
https://asciidoctor.org/docs/asciidoc-syntax-quick-reference/[AsciiDoc Syntax
Quick reference] und https://asciidoctor.org/docs/user-manual/[Asciidoctor User
Manual]

.Beispieldokumentation.adoc
[source,asciidoc]
----
= Beispiel-Dokumentation mit AsciiDoc <1>
Vorname Nachname <email> <2>
1.0, 01.09.2019 <3>
:toc: <4>
:toc-title: Inhaltsverzeichnis
// Platzhalter für weitere Dokumenten-Attribute <5>

== Einführung
Dieses Dokument dient im Praktikum als Spielwiese für das Kennenlernen und praktische Üben der Syntax von AsciiDoc.
----
<1> ... Titel des Dokumentes
<2> ... Autor und E-Mail
<3> ... Version, Datum
<4> ... Attribut für das Inhaltsverzeichnis
<5> ... Kommentar, wird später nicht generiert

[NOTE]
====
Mehrere Autoren werden in *eine Zeile* getrennt mit `;&#160;` (Semikolon
+ Leerzeichen) geschrieben. Namen können aus Vor-, Mittel- und Nachnamen
bestehen. Besteht ein Namensteil aus mehreren Teilen, müssen diese jeweils mit
`_` zu einer Gruppe zusammengefasst werden.

[source,asciidoc]
----
= Dokumenten-Titel
Vorname1_Vorname2 Nachname1_Nachname2 <email>; Vorname Nachname1_Nachname2_Nachname3 <email>; ...
----
====
--

. Aktivieren Sie in Visual Studio Code die parallele Vorschau des
  AsciiDoc-Dokumentes, siehe <<image_vscode_asciidoc>>, mit dem dritten Button
  oben rechts (geteiltes Fenster mit Lupe) oder direkt über folgende
  Tastenkombination: kbd:[Ctrl+K] kbd:[V] (macOS kbd:[cmd+K] kbd:[V]).
+
[#image_vscode_asciidoc]
.Screenshot VSCode mit AsciiDoc
image::screenshot_vscode_asciidoc.png[Screenshot VSCode mit AsciiDoc Vorschau, width=700]

. Speichern Sie die Änderungen in Ihrer Datei _Beispieldokumentation.adoc_.

{section-separator}

=== 4. Git-Workflow

.Überblick zum Git Workflow
image::grafik_git_workflow.jpg[Überblick zum Git Workflow, width=800]

==== Aufgabe 4.1 - Versionierung der Änderungen

Mit den folgenden Schritten nehmen Sie mithilfe von Visual Studio Code die
Änderungen in Ihrem Projekt/Workspace als neue Version in Ihr Git-Repository auf:

[#image_vscode_git]
.Screenshot VSCode mit AsciiDoc
image::screenshot_vscode_git.png[Screenshot VSCode mit Git-Integration, width=600]

. Aktivieren Sie über die Sidebar die Versionsverwaltungsansicht
  kbd:[Ctrl+Shift+G] ("View > Source Control")

. Im Bereich *CHANGES* (`git status`) sehen Sie alle _neuen_ *U* (unbeobachtet
  bzw. untracked/unverfolgt), _geänderten_ *M* (modifiziert) und _gelöschten_
  *D* (gelöscht) Dateien gegenüber ihrer aktuellen Repository-Version. Über das
  icon:plus[] Symbol (`git add`) bei den einzelnen Dateien oder im Bereich
  CHANGES können Sie einzelne oder alle Änderungen in den *INDEX* (Bereich
  *STAGED CHANGES*) aufnehmen.

. Im Bereich *STAGED CHANGES* sehen Sie alle im *INDEX* vorgemerkten geänderten
  Dateien. Über das icon:minus[] Symbol (`git rm --cached`, `git reset`) können
  diese wieder aus dem Index entfernt werden.

. Geben Sie in der *Textbox* (_Message ..._) einen kurzen Text über die
  Änderungen die Sie an Ihrem Projekt vorgenommen haben an.

. Mit kbd:[Enter] oder über das icon:check[] Symbol (`git commit`) können Sie
  jetzt alle im INDEX vorgemerkten Änderungen mit der angegebenen
  Commit-Message als neue Version in Ihr *lokales* Repository aufnehmen.

. Senden Sie jetzt den Stand Ihres *lokalen* Repositories zu Ihrem entfernten
  (_remote_) Repository auf GitHub. Dies erfolgt über "... > Pull, Push
  > Push" (`git push`).
+
Alternativ über den kbd:[Sync Changes] Button, welcher eine Kombination aus
Pull & Push ist.

. Schauen Sie sich im Webbrowser auf GitHub Ihre eben gemachten Änderungen in
  Ihrem Repository an.

TIP: Klicken Sie in der Versionsverwaltungsansicht (Source Control) auf den
jeweiligen Dateinamen (im Bereich CHANGES, STAGED CHANGES), öffnet sich ein
neuer Tab mit einer Gegenüberstellung (`git diff`) Ihrer Bearbeitung zur letzten
Version.

.Hinweise: Beispielhafter Ablauf mit Git-Kommandos im Terminal
[%collapsible]
====
=====
Initialer Schritt::
. `cd path/to/folder` <- Zum Verzeichnis wechseln in dem das Repository liegen soll
. `git clone \https://github.com/<account>/htwd-se-example-project.git` <- Lokale Kopie vom Remote-Repository erstellen (einmalig)

Arbeitsablauf::
. `cd htwd-se-example-project` <- In das Repository-Verzeichnis wechseln
. `git pull` <- Neue (mögliche) Version(en) vom Remote-Repository laden
. _... Projekt bearbeiten ..._
. `git status` / `git diff` <- Geänderte Dateien / Änderungen anzeigen
. `git add .` <- Alle Änderungen für neue Version vormerken
. `git commit -m "..."` <- Neue Version mit Commit Message erstellen
. `git log --oneline` <- Historie (einzeilig) anzeigen
. `git push` <- Neue Version(en) zum Remote-Repository senden
=====
====

{section-separator}

=== Extras

==== HTML/PDF-Generierung

Für die Generierung verwenden Sie im Terminal (PowerShell, Terminal in Visual
Studio Code: "Menü > View > Terminal") die Befehle `asciidoctor` bzw.
`asciidoctor-pdf`.

.HTML
[source,sh]
----
asciidoctor Beispieldokumentation.adoc
----

.PDF
[source,sh]
----
asciidoctor-pdf Beispieldokumentation.adoc
asciidoctor -r asciidoctor-pdf -b pdf Beispieldokumentation.adoc
----

Die fertig generierten Dokumente sehen dann wie folgt aus:

:imageTitle: Screenshot Generiertes HTML (links) und PDF (rechts)
:imageFile: screenshot_html_pdf.png
.{imageTitle}
image::{imageFile}["{imageTitle}", width=500]

.Hinweise zur Nutzung über die *ilux150*
[%collapsible]
====
=====
Das Generieren der AsciiDoc-Dokumente kann auch auf dem Login-Server *ilux150*
der Fakultät durchgeführt werden. Hier ist die entsprechende
Asciidoctor-Umgebung installiert.

[IMPORTANT]
======
Studenten des Moduls I925 (Studiengang Wirtschaftsingenieurwesen) haben keinen
Zugriff auf die ilux150.
======

*Variante 1 (erstmalig): Repository Clone auf der ilux150 nicht vorhanden*

. Loggen Sie sich per SSH oder über einen SSH-Client (Windows: bspw.
  https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html[Putty]) auf
  den Linux-Server *ilux150* ein:
+
[source,sh]
----
ssh s00000@ilux150.informatik.htw-dresden.de
----

. Passen Sie, falls noch nicht geschehen, Ihre Git-Konfiguration
  _.gitconfig_ an und setzen Ihren Namen und E-Mail.
+
[source,sh]
----
git config --global user.name "Vorname Nachname"
git config --global user.email s00000@informatik.htw-dresden.de
----

. Navigieren Sie zu dem Verzeichnis, in dem Sie eine Kopie Ihres Repositories
  *clonen* wollen.
+
[source,sh]
----
cd path/to/praktika/se/
----

. Clonen Sie Ihr Repository *htwd-se-example-project*:
+
[source,sh]
----
git clone https://github.com/<username>/htwd-se-example-project.git
----

. Wechseln Sie in das eben erstellte Verzeichnis *htwd-se-example-project*:
+
[source,sh]
----
cd htwd-se-example-project
----

. Generieren Sie die benötigten Dokumente:
+
--
.HTML
[source,sh]
----
asciidoctor Beispieldokumentation.adoc
----

.PDF
[source,sh]
----
asciidoctor -r asciidoctor-pdf -b pdf Beispieldokumentation.adoc
----
--

*Variante 2: Repository Clone auf der ilux150 vorhanden*

. Loggen Sie sich per SSH oder über einen SSH-Client (Windows: bspw.
  https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html[Putty]) auf
  den Linux-Server *ilux150* ein:
+
[source,sh]
----
ssh s00000@ilux150.informatik.htw-dresden.de
----

. Navigieren Sie zu Ihrem Projektverzeichnis *htwd-se-example-project*:
+
[source,sh]
----
cd path/to/praktika/se/htwd-se-example-project
----

. Aktualisieren Sie Ihr Repository auf den aktuellen Stand von GitHub:
+
[source,sh]
----
git pull
----

. Generieren Sie die benötigten Dokumente:
+
--
.HTML
[source,sh]
----
asciidoctor Beispieldokumentation.adoc
----

.PDF
[source,sh]
----
asciidoctor -r asciidoctor-pdf -b pdf Beispieldokumentation.adoc
----
--

[NOTE]
======
Arbeiten Sie in den Windows-Laboren auf Ihrem `U:\`-Studentenlaufwerke (SAMBA),
müssen Sie es auf der ilux150 nicht erst klonen, sondern navigieren direkt zu
Ihrem Repository Verzeichnis.
======
=====
====


==== Dateien ignorieren

Um zu verhindern, dass *ungewollte* oder *generierbare* Dateien, wie bspw.
HTML- und PDF-Dateien, in das Repository aufgenommen bzw. deren Änderungen
überwacht werden, müssen diese von Git ignoriert werden.

Dazu wird im Repository eine Datei _.gitignore_ angelegt, in der festgelegt wird,
welche Dateien, Verzeichnisse und Dateitypen von Git ignoriert werden sollen.

IMPORTANT: Die Datei _.gitignore_ muss in Ihr Repository aufgenommen werden, damit Sie für
jeden der Mitwirkenden gilt.

Um alle HTML- und PDF-Dateien im Projekt zu ignorieren, bieten sich folgende
Einträge in der _.gitignore_ an:

..gitignore
[source,gitignore]
----
*.html
*.pdf
----

Eine komplexere _.gitignore_ um HTML- und PDF-Dateien nur im *docs*-Verzeichnis
zu ignorieren:

..gitignore (komplexeres Beispiel)
[source,gitignore]
----
# ignore unwanted files <1>
*~
.DS_Store

# ignore special folder '.asciidoctor'
.asciidoctor/ <2>

# ignore files in the docs folder
docs/*.html <3>
docs/*.pdf
----
<1> mit `#` erhält man eine Kommentarzeile
<2> Dies ist ein spezielles Verzeichnis, welches Asciidoctor beim Generieren
(inkl. Vorschau) anlegt und temporäre Daten zwischenspeichert.
<3> ignoriert `\*.html` und `*.pdf` Dateien nur im *docs*-Verzeichnis


{section-separator}


==== Vorbereitung privater Rechner

Für die Bearbeitung der Aufgaben und des Belegprojekts benötigen Sie auf Ihrem
privaten Rechner folgende zusätzliche Software:

- *Git* mit eingerichteter Konfiguration (siehe <<section_git_config>>)
- *Visual Studio Code* mit der *AsciiDoc-Erweiterung*

Die Installation der *Asciidoctor*-Tools, für die Generierung von HTML- und
PDF-Dateien, ist nicht notwendig, da dies auch auf den Laborrechnern oder der
ilux150 durchgeführt werden kann.

TIP: Weitere Informationen zur link:install-and-config/[Installation und Konfiguration] unter
icon:windows[] Windows, icon:linux[] Linux und icon:apple[] macOS.

IMPORTANT: Der Umgang mit *Git*, *GitHub* und *Visual Studio Code* ist
Voraussetzung für die Bearbeitung der Praktikumsaufgaben und des
Belegprojektes. Bitte stellen Sie sicher, dass Sie die Grundlagen beherrschen
und die Werkzeuge auf Ihrem Rechner eingerichtet haben.

include::sitemap.inc.adoc[]
